import os
from editor_backend.settings.base import PROJECT_DIR

SECRET_KEY = 'SOMETHING_VERY_SEKRIT'
DEBUG = False
ALLOWED_HOSTS = []

#DATABASES = {
#}

STATIC_URL = '/static/'
STATIC_ROOT = os.path.join(PROJECT_DIR, 'static')
#STATICFILES_DIRS = (os.path.join(PROJECT_DIR, 'static'), )
